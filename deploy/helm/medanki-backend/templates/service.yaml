apiVersion: v1
kind: Service
metadata:
  name: {{ include "medanki-backend.fullname" . }}
  labels:
    {{- include "medanki-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "medanki-backend.selectorLabels" . | nindent 4 }}
